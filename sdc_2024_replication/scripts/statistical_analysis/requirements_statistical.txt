# Statistical Analysis Requirements
# Install with: micromamba run -n cohort_proj pip install -r requirements_statistical.txt

# Core (already installed)
pandas>=2.0
numpy>=1.24
scipy>=1.10
statsmodels>=0.14
matplotlib>=3.7
seaborn>=0.12

# Time Series (need to install)
arch>=6.0          # GARCH models, structural breaks
ruptures>=1.1      # Change point detection
pmdarima>=2.0      # Auto-ARIMA

# Panel Data (need to install)
linearmodels>=5.0  # Fixed effects, IV, dynamic panel GMM

# Machine Learning (need to install)
scikit-learn>=1.3  # Elastic net, random forest, clustering

# Optional: Enhanced capabilities
# xgboost>=2.0     # Gradient boosting (large install)
# shap>=0.43       # Feature importance interpretability
# lifelines>=0.27  # Survival analysis

# Bayesian Inference (Phase B - approved 2026-01-01)
pymc>=5.10         # Bayesian VAR, Minnesota prior
arviz>=0.17        # Bayesian diagnostics, posterior visualization

# Causal Inference (may need custom implementations)
# No well-maintained Python synthetic control package
# Will implement manually or use statsmodels
