# .env.example - Environment variables for cohort_projections
#
# Copy this file to .env and fill in your values.
# The .env file is loaded automatically by direnv (via .envrc).
# NEVER commit .env to git - it may contain secrets.
#
# See: docs/guides/configuration-reference.md for full documentation

# =============================================================================
# CENSUS API (Optional)
# =============================================================================
# Required for: fetching data from Census Bureau APIs
# How to get: https://api.census.gov/data/key_signup.html
# Without this key, API requests are rate-limited to ~500/day.
# With a key, rate limits increase significantly.

CENSUS_API_KEY=

# =============================================================================
# GOOGLE BIGQUERY (Optional)
# =============================================================================
# Required for: accessing Census/demographic data via BigQuery public datasets
# See: docs/BIGQUERY_SETUP.md for setup instructions
#
# Note: These can also be set in config/projection_config.yaml.
# Environment variables override config file values.

# Your Google Cloud Platform project ID
GCP_PROJECT_ID=antigravity-sandbox

# Path to service account JSON key file
# See docs/BIGQUERY_SETUP.md for creating a service account
GCP_CREDENTIALS_PATH=~/.config/gcloud/cohort-projections-key.json

# =============================================================================
# POSTGRESQL DATABASE (Optional)
# =============================================================================
# Required for: SDC 2024 replication database features
# Used by: sdc_2024_replication/scripts/database/
#
# If DEMOGRAPHY_DATABASE_URL is set, it takes precedence over individual vars.

# Full connection URL (optional - overrides individual vars below)
# DEMOGRAPHY_DATABASE_URL=postgresql://user:pass@localhost:5432/demography_db

# Individual connection parameters
DEMOGRAPHY_DB_NAME=demography_db
DEMOGRAPHY_DB_USER=
DEMOGRAPHY_DB_PASSWORD=
DEMOGRAPHY_DB_HOST=localhost
DEMOGRAPHY_DB_PORT=5432

# =============================================================================
# CODEBASE CATALOG DATABASE (Optional)
# =============================================================================
# Required for: Repository intelligence / code inventory features
# Used by: libs/codebase_catalog/
#
# These are standard PostgreSQL environment variables.
# Only needed if using the codebase catalog features.

# PGHOST=localhost
# PGPORT=5432
# PGDATABASE=cohort_projections_meta
# PGUSER=

# =============================================================================
# SDC 2024 REPLICATION (Optional)
# =============================================================================
# Configuration for the SDC 2024 statistical analysis pipeline

# Data source mode: auto, file, or database
# - auto: tries database first, falls back to file
# - file: always use local CSV files
# - database: always use PostgreSQL
SDC_ANALYSIS_DATA_SOURCE=auto

# Duration analysis tag (set by pipeline, usually not set manually)
# SDC_DURATION_TAG=

# =============================================================================
# MATPLOTLIB BACKEND (Development)
# =============================================================================
# Set to 'Agg' for headless/CI environments (no display)
# Leave unset for interactive plotting

# MPLBACKEND=Agg
