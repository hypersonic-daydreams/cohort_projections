# Draft Numbers For Layout

These March 2026 layout figures were built from the older three-scenario setup
and are stale under ADR-065. Do not reuse them as final public copy.

Refresh every numeric callout after CF-001 and the baseline-only rerun. The
public package should use Baseline (CBO-Adjusted) only.

## What Needs Regeneration

- State key years for the public baseline path.
- Baseline regional change figures.
- Baseline county growth highlights.
- Baseline age-structure figures.
- Baseline sex-ratio figures.

## Layout Notes

- The public workbook uses `State Key Years` instead of `Scenario Summary`.
- The public CSV is baseline-only and should not include
  `recent_trend_continuation`, `restricted_growth`, or `high_growth`.
- Any old numbers in this folder are layout placeholders, not approved public
  figures.
