# project-utils

Lightweight configuration and logging utilities used across the `cohort_projections` codebase.

This package is vendored into this repository to keep the development environment reproducible
when the original `~/workspace/libs/project_utils` path is not available.
